{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist", ".output"]
		},
		"lint": {},
		"deploy": {
			"dependsOn": ["build", "lint"]
		}
	}
}
