{
	"name": "pandapaco-drawing-stats",
	"license": "MIT",
	"private": true,
	"type": "module",
	"workspaces": [
		"app",
		"packages/*"
	],
	"lint-staged": {
		"app": "yarn prettier:write",
		"packages/**": "yarn prettier:write"
	},
	"scripts": {
		"cleanup": "node clean.js && yarn",
		"dev": "turbo dev",
		"build": "turbo build",
		"postinstall": "cd app && nuxt prepare",
		"prepare": "husky install",
		"prettier:check": "prettier --check .",
		"prettier:write": "prettier --write ."
	},
	"devDependencies": {
		"concurrently": "^7.6.0",
		"husky": "^8.0.0",
		"lint-staged": "^13.1.2",
		"prettier": "^2.8.3",
		"turbo": "^1.8.2"
	},
	"engines": {
		"node": ">16.0.0",
		"npm": ">= 8.0.0"
	}
}
