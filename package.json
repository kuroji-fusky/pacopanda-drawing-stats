{
  "private": true,
  "name": "pacopanda-drawing-stats",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "prepare": "husky install"
  },
  "workspaces": [
    "src/client/**"
  ],
  "lint-staged": {
    "client/**": "yarn prettier --write .",
    "server/**/!(*cache).json": "yarn prettier --write .",
    "*.md": "yarn prettier --write .",
    "*.{js,ts,svelte}": "eslint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo I love Jungkook"
    }
  },
  "devDependencies": {
    "@turbo/gen": "latest",
    "husky": "^8.0.0",
    "lint-staged": "^15.0.1",
    "prettier": "^3.0.3",
    "prettier-plugin-svelte": "^3.0.3",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
