{
  "name": "pandapaco-drawing-stats",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/shared",
    "puppeteer"
  ],
  "scripts": {
    "cleanup": "node ./scripts/cleanBloat.js && yarn",
    "dev:web": "yarn workspace @pds/website dev",
    "postinstall": "cd ./apps/website && nuxt prepare",
    "prepare": "husky install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "husky": "^8.0.0",
    "prettier": "^2.8.3"
  }
}
